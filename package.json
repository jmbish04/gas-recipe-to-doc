{
  "name": "gas-project",
  "version": "1.0.0",
  "description": "Google Apps Script project managed with clasp",
  "scripts": {
    "deploy": "clasp push -f && clasp deploy -d \"Auto-deployment from GitHub\""
  },
  "devDependencies": {
    "@google/clasp": "^2.4.2"
  }
}
