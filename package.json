{
  "name": "gas-project",
  "version": "1.0.0",
  "description": "Google Apps Script project managed with clasp",
  "scripts": {
    "lint": "eslint src/**/*.ts",
    "push": "clasp push -f",
    "deploy": "npm run push && clasp deploy -d \"Auto-deployment from GitHub\""
  },
  "devDependencies": {
    "@google/clasp": "^2.4.2"
  }
}
